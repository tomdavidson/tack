# tack-manifest.yml for the example fixture package.
# Declares render vars source and merge target.
files:
  greeting.tera.yml:
    vars_from: ../../tackrc.yml
  clippy-cargo.concat.toml:
    target: Cargo.toml
    enforced:
      - workspace.lints.clippy.unwrap_used
